# ---------------------------------------------------------------------------
# This file is written by the LOCAL Flask classroom demo (app.py) when a
# student submits the login form. It is git-ignored and must never be
# committed or deployed: it contains exactly what a phishing operator would
# harvest from victims.
#
# The Netlify (static) version of this demo never writes or transmits this
# data — everything stays in the visitor's own browser and is wiped after
# the educational reveal.
# ---------------------------------------------------------------------------
